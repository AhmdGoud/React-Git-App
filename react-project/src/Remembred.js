// jsx as a prop, we destrucre the children eles and pass it to the ele as a component
export default function Remembred({children}){
    return(
        <div>
            {children}
        </div>
    )
}